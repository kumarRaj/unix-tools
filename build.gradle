apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'rajkum.unixtools.cli.WcMain'

run {
    args "build.xml"
}
repositories { mavenCentral() }

dependencies {
    testCompile 'junit:junit:4.11'
}